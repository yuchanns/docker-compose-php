# docker-compose-php-env

## Up a compose
```sh
docker-compose up --build # with --build tag at the first time
```

## Down a compose
```sh
docker-compose down
```

## Delete a down compose
```sh
docker-compose rm -f
```

